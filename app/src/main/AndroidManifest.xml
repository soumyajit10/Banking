<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easybanking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyBanking">
        <activity android:name=".UI.UsersList"
            android:exported="true"/>
        <activity android:name=".UI.UserData"
            android:exported="true"/>
        <activity android:name=".UI.TransactionHistory"
            android:exported="true"/>
        <activity android:name=".UI.SendToUserList"
            android:exported="true"/>
        <activity android:name=".UI.HomeScreenActivity"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <meta-data
        android:name="preloaded_fonts"
        android:resource="@array/preloaded_fonts" />
    </application>

</manifest>